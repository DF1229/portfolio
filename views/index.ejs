<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Daan's portfolio</h1>

    <% if (locals.debugMsg) { %>
    <p style="color: red"><%= locals.debugMsg %></p>
    <% } %>

    Logged in as <b><%= locals.user.username %></b> <% if (locals.user.admin) { %> [ADMIN] <% } %><br>
    <i><%= locals.user.email %></i>
    <br>
</body>
</html>