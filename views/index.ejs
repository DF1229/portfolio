<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Daan's portfolio</h1>

    <% if (locals.debugMsg) { %>
    <p><%= locals.debugMsg %></p>
    <% } %>

    Logged in as <%= locals.user.username %>.<br>
    <%= locals.user.email %>
    <br>
    <% if (locals.user.admin) { %>
        Logged in as admin.
        Click <a href="/register">here</a> to register a new user.
    <% } %>
</body>
</html>